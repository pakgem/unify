This PR adds a tiny minify build step and checks in minified JS artifacts for faster Webflow loads.

- Adds `terser` devDependency
- Adds `npm run build` / `build:min` (runs `scripts/minify.mjs`)
- Generates `JS/global.min.js` and `JS/home.min.js`

Notes:
- No functional changes intended; minified output should be equivalent.
- Next step (separate PR): update Webflow script embeds to point at the `.min.js` assets (versioned tag URL).

Quality gate: PASS (ran `/Users/geminpak/clawd/scripts/quality-gate.sh` in this repo).
